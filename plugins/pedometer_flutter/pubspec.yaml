name: pedometer_flutter
description: A Flutter library for tracking steps and calculating step rate.
version: 0.0.1
homepage: https://open.popular.health
publish_to: none

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  collection: ^1.17.0
  flutter:
    sdk: flutter
  iirjdart: ^0.1.0
  matrix2d: ^1.0.4
  scidart: ^0.0.2-dev.12
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.22.0
  very_good_analysis: ^4.0.0+1
